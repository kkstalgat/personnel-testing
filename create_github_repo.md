# 🔧 Создание репозитория на GitHub

## Проблема
Ошибка `remote: Repository not found` означает, что репозиторий `personnel-testing` еще не создан на GitHub.

## ✅ Решение: Создайте репозиторий вручную

### Шаг 1: Откройте GitHub
Перейдите на: **https://github.com/new**

Или:
1. Зайдите на https://github.com
2. Нажмите **"+"** в правом верхнем углу
3. Выберите **"New repository"**

### Шаг 2: Заполните форму

**Repository name**: `personnel-testing`

**Description** (опционально): `Система тестирования персонала`

**Visibility**:
- ✅ **Public** - если хотите открытый репозиторий
- ✅ **Private** - если хотите приватный репозиторий

**ВАЖНО - НЕ ставьте галочки на:**
- ❌ Add a README file
- ❌ Add .gitignore  
- ❌ Choose a license

(У нас уже есть все необходимые файлы)

### Шаг 3: Создайте репозиторий
Нажмите кнопку **"Create repository"**

### Шаг 4: После создания выполните команду

```bash
git push -u origin main
```

---

## 🔐 Если потребуется аутентификация

При выполнении `git push` GitHub может запросить:

**Username**: `kkstalgat`

**Password**: Используйте **Personal Access Token** (не обычный пароль!)

### Как создать Personal Access Token:

1. Откройте: https://github.com/settings/tokens
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Заполните:
   - **Note**: `Django Project Access`
   - **Expiration**: Выберите срок (или `No expiration`)
   - **Select scopes**: Отметьте ✅ **`repo`** (полный доступ к репозиториям)
4. Нажмите **"Generate token"**
5. **ВАЖНО**: Скопируйте токен сразу (он показывается только один раз!)
6. Используйте этот токен как пароль при `git push`

---

## 🚀 Альтернатива: Создание через GitHub CLI (если установлен)

Если у вас установлен GitHub CLI (`gh`), можно создать репозиторий из командной строки:

```bash
# Проверить, установлен ли GitHub CLI
gh --version

# Если установлен, авторизоваться
gh auth login

# Создать репозиторий
gh repo create personnel-testing --public --source=. --remote=origin --push
```

---

## ✅ После успешного создания репозитория

Проверьте, что код загружен:
1. Откройте: https://github.com/kkstalgat/personnel-testing
2. Вы должны увидеть все файлы проекта
3. Убедитесь, что `venv/`, `.env`, `db.sqlite3` **НЕ** загружены (они в .gitignore)
