# 📤 Загрузка проекта на GitHub

## Шаг 1: Установка Git (если не установлен)

### Windows:
Скачайте и установите Git с официального сайта: https://git-scm.com/download/win

Или используйте установщик через winget:
```powershell
winget install Git.Git
```

### Проверка установки:
```bash
git --version
```

---

## Шаг 2: Создание репозитория на GitHub

1. Войдите в свой аккаунт GitHub: https://github.com
2. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
3. Заполните форму:
   - **Repository name**: `personnel-testing` (или другое имя)
   - **Description**: "Система тестирования персонала"
   - **Visibility**: 
     - ✅ **Public** (открытый) - если хотите, чтобы код был виден всем
     - ✅ **Private** (приватный) - если код должен быть скрыт
   - **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license" (у нас уже есть .gitignore)
4. Нажмите **"Create repository"**

---

## Шаг 3: Инициализация Git в проекте

Откройте терминал (PowerShell, CMD или Git Bash) в папке проекта и выполните:

```bash
# 1. Инициализация Git репозитория
git init

# 2. Добавление всех файлов (кроме тех, что в .gitignore)
git add .

# 3. Первый коммит
git commit -m "Initial commit: Django personnel testing system"

# 4. Переименование ветки в main (если нужно)
git branch -M main
```

---

## Шаг 4: Подключение к GitHub и загрузка

### Вариант 1: Через HTTPS (проще для начала)

```bash
# Замените YOUR_USERNAME и YOUR_REPO на ваши данные
# Например: https://github.com/username/personnel-testing.git

git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# Загрузка кода на GitHub
git push -u origin main
```

**Примечание**: При первом push GitHub попросит ввести логин и пароль. 
Для пароля используйте **Personal Access Token** (см. ниже).

### Вариант 2: Через SSH (рекомендуется для постоянной работы)

#### 4.1. Генерация SSH ключа (если еще нет):

```bash
# Проверить, есть ли уже ключи
ls -al ~/.ssh

# Если нет, создать новый ключ
ssh-keygen -t ed25519 -C "your_email@example.com"
# Нажмите Enter для всех вопросов (или укажите пароль)

# Показать публичный ключ
cat ~/.ssh/id_ed25519.pub
```

#### 4.2. Добавление SSH ключа в GitHub:

1. Скопируйте содержимое публичного ключа (вывод команды выше)
2. GitHub → Settings → SSH and GPG keys → New SSH key
3. Вставьте ключ и сохраните

#### 4.3. Подключение через SSH:

```bash
# Замените YOUR_USERNAME и YOUR_REPO
git remote add origin git@github.com:YOUR_USERNAME/YOUR_REPO.git

# Загрузка кода
git push -u origin main
```

---

## 🔑 Создание Personal Access Token (для HTTPS)

Если используете HTTPS и GitHub требует токен:

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Настройки:
   - **Note**: "Django Project Access"
   - **Expiration**: Выберите срок (или No expiration)
   - **Scopes**: Отметьте `repo` (полный доступ к репозиториям)
4. Generate token
5. **ВАЖНО**: Скопируйте токен сразу (он показывается только один раз!)
6. При push используйте:
   - **Username**: ваш GitHub логин
   - **Password**: вставьте токен (не обычный пароль!)

---

## 📝 Полная последовательность команд (копировать и выполнить)

```bash
# 1. Инициализация
git init

# 2. Добавление файлов
git add .

# 3. Первый коммит
git commit -m "Initial commit: Django personnel testing system"

# 4. Переименование ветки
git branch -M main

# 5. Добавление удаленного репозитория (замените URL!)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
# или для SSH:
# git remote add origin git@github.com:YOUR_USERNAME/YOUR_REPO.git

# 6. Загрузка на GitHub
git push -u origin main
```

---

## ✅ Проверка успешной загрузки

1. Откройте ваш репозиторий на GitHub в браузере
2. Вы должны увидеть все файлы проекта
3. Проверьте, что `.env` и `venv/` **НЕ** загружены (они в .gitignore)

---

## 🔄 Последующие обновления

После внесения изменений в проект:

```bash
# 1. Проверить изменения
git status

# 2. Добавить измененные файлы
git add .

# 3. Создать коммит с описанием изменений
git commit -m "Описание того, что изменили"

# 4. Загрузить на GitHub
git push
```

---

## 🛠️ Полезные команды Git

```bash
# Просмотр статуса
git status

# Просмотр изменений
git diff

# Просмотр истории коммитов
git log

# Отмена изменений в файле (до git add)
git checkout -- filename

# Отмена добавления файла (после git add, до commit)
git reset HEAD filename

# Изменение последнего коммита (если забыли что-то добавить)
git add forgotten_file
git commit --amend --no-edit
```

---

## ⚠️ Важные замечания

1. **НЕ коммитьте `.env` файл** - он содержит секретные ключи!
2. **НЕ коммитьте `venv/`** - виртуальное окружение
3. **НЕ коммитьте `db.sqlite3`** - локальная база данных
4. **Проверяйте `.gitignore`** перед каждым коммитом

---

## 🐛 Решение проблем

### Ошибка: "remote origin already exists"
```bash
git remote remove origin
git remote add origin YOUR_REPO_URL
```

### Ошибка: "failed to push some refs"
```bash
# Если на GitHub уже есть файлы (README и т.д.)
git pull origin main --allow-unrelated-histories
# Разрешите конфликты, если есть
git push -u origin main
```

### Ошибка: "Permission denied"
- Для HTTPS: Проверьте Personal Access Token
- Для SSH: Проверьте, что SSH ключ добавлен в GitHub

### Забыли добавить файл в .gitignore
```bash
# Удалить файл из Git (но оставить локально)
git rm --cached .env
git commit -m "Remove .env from repository"
git push
```

---

## 📚 Дополнительные ресурсы

- [Официальная документация Git](https://git-scm.com/doc)
- [GitHub Guides](https://guides.github.com/)
- [Git Cheat Sheet](https://education.github.com/git-cheat-sheet-education.pdf)
