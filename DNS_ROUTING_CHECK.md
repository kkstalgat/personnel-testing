# 🔍 Проверка маршрутизации DNS и сети

## Проблема: сайт работает только через VPN

Если сайт работает через VPN, но без VPN показывает дефолтную страницу Nginx, проблема в маршрутизации или DNS.

---

## 📋 Проверка DNS

### 1. Проверка с вашего компьютера (без VPN)

```bash
# Проверьте, куда резолвится домен
nslookup chartesting.kus.kz

# Или используйте
host chartesting.kus.kz

# Или используйте dig
dig chartesting.kus.kz
```

**Ожидаемый результат:** Домен должен резолвиться на `192.168.100.159`

**Если резолвится на другой IP:**
- DNS настроен неправильно
- Нужно настроить правильный DNS для домена

### 2. Проверка с сервера

```bash
# На сервере проверьте DNS
nslookup chartesting.kus.kz

# Проверьте внешний IP сервера
curl ifconfig.me
# или
curl -4 ifconfig.me

# Проверьте все IP адреса сервера
ip addr show
# или
hostname -I
```

---

## 🌐 Проверка сетевой маршрутизации

### 1. Проверка доступности сервера

С вашего компьютера (без VPN):

```bash
# Проверьте доступность по IP
ping 192.168.100.159

# Проверьте доступность порта 80
telnet 192.168.100.159 80
# или
nc -zv 192.168.100.159 80
# или
curl -I http://192.168.100.159
```

**Если недоступно:**
- `192.168.100.159` - это внутренний IP
- Без VPN этот IP недоступен из интернета
- Нужен внешний IP или VPN для доступа

### 2. Проверка внешнего доступа

Если сервер должен быть доступен из интернета:

```bash
# На сервере проверьте внешний IP
curl ifconfig.me

# Проверьте, открыт ли порт 80 во внешнем интерфейсе
sudo ss -tlnp | grep :80

# Проверьте firewall
sudo ufw status
# или
sudo iptables -L -n
```

---

## 🔧 Возможные решения

### Вариант 1: Домен указывает на другой сервер

Если без VPN домен резолвится на другой IP (не 192.168.100.159):

1. **Проверьте DNS записи домена:**
   - A-запись должна указывать на правильный IP
   - Возможно, есть внешний IP, который нужно настроить

2. **Настройте DNS правильно:**
   ```
   chartesting.kus.kz -> [внешний IP сервера]
   ```

3. **На сервере с внешним IP настройте Nginx:**
   - Создайте конфигурацию для этого IP
   - Или проксируйте запросы на 192.168.100.159

### Вариант 2: Нужен внешний IP

Если `192.168.100.159` - это только внутренний IP:

1. **Настройте внешний IP для сервера**
2. **Настройте DNS для домена:**
   ```
   chartesting.kus.kz -> [внешний IP]
   ```
3. **На сервере настройте Nginx для обработки внешних запросов:**
   ```nginx
   server {
       listen 80 default_server;
       server_name chartesting.kus.kz _;
       ...
   }
   ```

### Вариант 3: Прокси/балансировщик перед сервером

Если есть прокси или балансировщик перед сервером:

1. **Настройте прокси для передачи запросов на 192.168.100.159**
2. **Или настройте прокси для обработки домена chartesting.kus.kz**

---

## 🛠️ Команды для диагностики

### На вашем компьютере (без VPN)

```bash
# 1. Проверьте DNS
nslookup chartesting.kus.kz
host chartesting.kus.kz

# 2. Проверьте доступность по IP
ping 192.168.100.159
curl -I http://192.168.100.159

# 3. Проверьте доступность по домену
curl -I http://chartesting.kus.kz
curl -v http://chartesting.kus.kz
```

### На сервере

```bash
# 1. Проверьте все IP адреса
ip addr show
hostname -I

# 2. Проверьте внешний IP
curl ifconfig.me

# 3. Проверьте, что Nginx слушает на всех интерфейсах
sudo ss -tlnp | grep :80

# 4. Проверьте конфигурацию Nginx
sudo nginx -T | grep -A 5 "listen 80"

# 5. Проверьте логи Nginx
sudo tail -n 50 /var/log/nginx/access.log
sudo tail -n 50 /var/log/nginx/error.log
```

---

## 📝 Что проверить

1. ✅ **DNS без VPN** - куда резолвится домен?
2. ✅ **Доступность 192.168.100.159** - доступен ли этот IP без VPN?
3. ✅ **Внешний IP сервера** - есть ли внешний IP?
4. ✅ **Firewall** - открыт ли порт 80?
5. ✅ **Конфигурация Nginx** - слушает ли на всех интерфейсах (0.0.0.0)?

---

## 💡 Быстрое решение

Если `192.168.100.159` - это внутренний IP, и нужен доступ из интернета:

1. **Узнайте внешний IP сервера:**
   ```bash
   curl ifconfig.me
   ```

2. **Настройте DNS:**
   - A-запись `chartesting.kus.kz` должна указывать на внешний IP

3. **На сервере убедитесь, что Nginx слушает на всех интерфейсах:**
   ```nginx
   listen 80 default_server;
   ```

4. **Откройте порт 80 в firewall:**
   ```bash
   sudo ufw allow 80/tcp
   ```
